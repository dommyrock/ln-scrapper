use urlencoding::decode;
// urls.iter().map(|u| {
//    decode(u).expect("UTF-8").into_owned()

// }).collect::<Vec<String>>().iter().for_each(|f|
//    {
//        //Stripp everything after '&trackingId'
//        let cleaned = f.split("&trackingId").collect::<Vec<&str>>()[0].trim_end_matches("=");
//        println!("{cleaned}")
       
//    }
// );a